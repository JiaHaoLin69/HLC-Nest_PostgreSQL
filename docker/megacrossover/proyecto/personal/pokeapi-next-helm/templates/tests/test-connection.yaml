apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pokeapi-next-helm.fullname" . }}-test-connection"
  labels:
    {{- include "pokeapi-next-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pokeapi-next-helm.fullname" . }}:{{ .Values.app.service.port }}']
  restartPolicy: Never
