apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nest-con-helm.fullname" . }}-test-connection"
  labels:
    {{- include "nest-con-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: test-app
      image: busybox
      command: ['wget']
      args: ['{{ include "nest-con-helm.fullname" . }}:{{ .Values.app.service.port }}']
    - name: test-postgres
      image: busybox
      command: ['sh', '-c']
      args:
        - 'echo "Checking PostgreSQL connection..." && nc -zv {{ .Values.postgres.name }} {{ .Values.postgres.service.port }}'
  restartPolicy: Never
